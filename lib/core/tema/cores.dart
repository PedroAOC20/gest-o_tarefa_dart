import 'package:flutter/material.dart';

class Cores {
  // Cores principais
  static const Color fundoPrincipal = Color(0xFF0A0A23);
  static const Color preto = Color(0xFF000000);

  // Cores funcionais
  static const Color baixaPrioridade = Color(0xFF00BFFF);
  static const Color importante = Color(0xFFFFD700);
  static const Color urgente = Color(0xFFFF4500);
  static const Color concluido = Color(0xFF32CD32);

  // Cores adicionais para UI
  static const Color azulDestaque = Color(0xFF3949AB);
  static const Color fundoCard = Color(0xFF1A1A2E); // Fundo para cards
  static const Color fundoInput = Color(0xFF16213E); // Fundo para inputs
  static const Color bordaCard = Color(0xFF2A2A5A); // Borda para cards
  static const Color bordaInput = Color(0xFF2A2A5A); // Borda para inputs
  static const Color divider = Color(0xFF333366); // Divisores
  static const Color iconeTarefa = Color(0xFF7E84FF); // Ícones
}
